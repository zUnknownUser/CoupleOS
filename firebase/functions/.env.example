# Copy to .env before deploying.

# Rejects callable requests without a valid App Check token.
# Keep this false until the iOS build shipping App Attest is the only one in the
# field and the Firebase console App Check dashboard shows verified traffic.
ENFORCE_APP_CHECK=false
